# ChatGPT Круг 5
Дата: 2026-06-22 23:40:30.215720

ТИП: РЕВЬЮ

Найденные ошибки/коллизии

Блок restart_medic в белом списке логически опасен

launchctl kickstart -k com.neva.medic запускается из самого neva_medic.py.

Это самоперезапуск через supervisor.

Возможны гонки между старым и новым процессом до освобождения lock.

Не блокирующее замечание, но источник редких ложных crash-loop.

Boot-счётчик растёт бесконечно

neva_medic_boots.txt никогда не очищается.

Через месяцы/годы файл станет большим.

Нужно оставлять только записи последних 24 часов или последних N запусков.

Возможна авария на повреждённом boot-файле

Python
Запустить
boots = [float(l) for l in BOOT_LOG.read_text().splitlines() if l]

Одна битая строка → ValueError → падение медика.

Нужен try/except на каждую запись.

Heartbeat пишется неатомарно

Python
Запустить
HEARTBEAT.write_text(...)

status_dot может прочитать файл в момент записи.

Сейчас предусмотрен только CORRUPTED.

Лучше писать через .tmp + os.replace().

Нет очистки heartbeat при штатном завершении

Если процесс завершился корректно, heartbeat остаётся.

Проверка PID спасает от большинства случаев.

Но при быстром реиспользовании PID ОС возможна ложная интерпретация.

Желательно писать статус stopping.

Функция clear_logs может выбросить исключение

Python
Запустить
p.unlink()

Файл может быть открыт друг