# DeepSeek — THERMAL-001-R1
Дата: 2026-06-23 10:40:20.453455

АУДИТ ThermalGuard NEVA — КРУГ 1 (только вопросы)
ВОПРОСЫ ПО КОДУ

Q-КОД-1: В FSM из 9 состояний, какой конкретный код инициирует переход из DEGRADED в HEALTHY, и почему при mem=76%, swap=1.5GB, pressure=nominal этот переход не срабатывает — какое условие в коде блокирует переход?

Q-КОД-2: В двух FAIL тестах race condition — какие именно гонки детектируются (чтение/запись thermal_state.json, доступ к powermetrics, или состояние ollama), и какой механизм синхронизации используется/отсутствует в проблемных участках?

Q-КОД-3: Как в коде обрабатывается ситуация powermetrics_available=false при вызове функций, ожидающих p=nominal(0) — какие ветки кода выполняются, и где именно происходит подмена p=None на дефолтные значения?

Q-КОД-4: Какая логика в коде определяет расхождение ollama_available=false при ollama_serve_up=true — через какой таймаут/проверку здоровья принимается решение, и где в коде это зафиксировано?

Q-КОД-5: Как ThrottleInterval=30 влияет на интервалы между проверками состояний в self-test, и может ли этот интервал маскировать переходные процессы, приводящие к race condition в FAIL тестах?

ВОПРОСЫ ПО АРХИТЕКТУРЕ

Q-АРХ-1: Какое место ThermalGuard занимает в иерархии NEVA — является ли он подчинённым оркестратора, независимым стражем, или координатором между моделями и системой, и как это зафиксировано в схеме взаимодействия компонентов?

Q-АРХ-2: Почему kill-action при CRITICAL не реализован — это осознанное архитектурное решение (например, делегировано Medic), техническое ограничение (отсутствие прав), или незавершённый этап разработки, и где в архитектуре должен быть этот механизм?

Q-АРХ-3: При частично реализованном UDS канале ThermalGuard↔Medic — какие именно сообщения уже проходят через UDS, а какие через файлы, и какое архитектурное правило определяет выбор транспорта для каждого типа события?

Q-АРХ-4: Кто кем управляет в паре ThermalGuard↔Medic — ThermalGuard вызывает Medic при проблемах, Medic опрашивает ThermalGuard, или это двунаправленное управление с приоритетами,