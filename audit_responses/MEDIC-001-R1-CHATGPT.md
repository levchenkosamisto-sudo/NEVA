# ChatGPT
Дата: 2026-06-22 22:34:17.358438

ТИП: РЕВЬЮ

КРУГ 1 — ВОПРОСЫ

Что происходит, если neva_status_dot.py ошибочно считает Медик мёртвым, пока основной процесс ещё работает? Есть ли защита от запуска второго экземпляра neva_medic.py?

Где реализована гарантия, что restart_medic не создаёт цикл:

Медик диагностирует себя,

перезапускает себя,

после старта снова видит ту же проблему,

снова перезапускает себя?

Как определяется факт «живого» медика?
Сейчас используется наличие процесса и свежесть heal_end.
Почему не используется отдельный heartbeat-файл или health-endpoint самого медика?

Что произойдёт, если:

процесс существует,

лог обновляется,

но heal_cycle() завис внутри ai_diagnose() или сетевого вызова?

Каким образом система отличает рабочий цикл от частично зависшего состояния?

Почему watchdog (neva_status_dot.py) использует собственный nohup-запуск, а не восстановление через launchctl kickstart для com.neva.medic?

Не приводит ли это к двум разным механизмам управления жизненным циклом одного процесса?

Как предотвращается ситуация, когда одновременно срабатывают:

launchd KeepAlive,

watchdog status_dot,

ручной ремонт оператора?

Есть ли блокировка по PID-файлу, lock-файлу или иной механизм защиты от гонки запусков?

Что произойдёт при повреждении или недоступности директории:

medic_knowledge/

Сможет ли Медик продолжить базовую диагностику без базы знаний или работа полностью остановится?

Что происходит, если MCP сервер :9000 отвечает, но отдаёт некорректные данные?

Проверяется ли только факт HTTP-ответа или также валидность структуры ответа (status=ok, версия, формат JSON)?

Как Медик отличает:

временную деградацию компонента,

хроническую проблему,

ложноположительное срабатывание?

Где хранится история наблюдений и каков критерий перехода в chronic?

Есть ли защита от ошибочного playbook?

Например:

AI поставил неверный диагноз,

confidence оказался достаточным,

был выполнен неправильный ремонт.

Есть ли dry-run, подтверждение, ограничение опасных команд или откат после неудачного ремонта?

Что произойдёт при заполнении диска?

Практически все проверки завязаны на логи:

medic.log

auditor.log

status_dot.log

Как Медик диагностирует невозможность записи логов и чем это отличается от собственного зависания?

Кто контролирует сам watchdog (neva_status_dot.py) кроме launchd?

Если процесс жив, но его логика зависла и он перестал выполнять проверки, каким образом это обнаруживается?

Есть ли единый источник истины для состояния системы?

Сейчас используются:

процессы,

порты,

логи,

traffic_light,

chronic,

AnyBar.

Какой из этих источников считается главным при конфликте показаний?

Что произойдёт после перезагрузки Mac в сценарии:

сеть недоступна,

AI-провайде