# Deepseek — CURSOR-001 Круг 1
Дата: 2026-06-26 07:52:52.538427

Как Cursor получает актуальную версию tasks.md — через автоматический git pull перед началом работы, через прямое чтение файловой системы или через ручное копирование текста задачи в интерфейс Cursor?

Как Claude узнаёт, что в questions.md появился новый вопрос от Cursor — он периодически сканирует репозиторий, получает уведомление от Директора или проверяет файл вручную по запросу?

Кто физически запускает команду pytest на Этапе 2 (RED) и на Этапе 3 (после каждой задачи) — Cursor (как исполнитель) или Claude (как архитектор), и кто фиксирует результат в test_report.md?

Как Директор отслеживает текущее состояние процесса между этапами — через статус-файл, через комментарии в pull request, через дашборд или он вручную проверяет завершённость каждого артефакта?

Если Cursor не может выполнить задачу из tasks.md (например, из-за неполной спецификации или технического ограничения), каков протокол — он останавливается и пишет в questions.md, переходит к следующей задаче или помечает задачу как заблокированную?

Где физически хранятся все артефакты (spec.md, design.md, tasks.md, tests_spec.py, код, review.md, test_checklist.md) — в корне одного репозитория, в отдельных папках по этапам или в отдельных ветках git?

Нужен ли отдельный git-репозиторий для каждой новой программы, разрабатываемой по этому пайплайну, или все программы хранятся в одном общем репозитории NEVA с подпапками?

Как AGENTS.md попадает в контекст Cursor — он автоматически подгружается при старте сессии, его нужно вручную вставить в промпт или Cursor читает его из репозитория по команде?

Как Claude проверяет, что тесты в tests_spec.py действительно падают (RED), а не проходят или не выполняются из-за син