АУДИТ CURSOR-001 — КРУГ 4
Тема: Доработанное решение после замечаний Круга 3
Директор: Серж | Архитектор: Claude | Дата: 2026-06-26

---

ДОРАБОТКИ ПОСЛЕ КРУГА 3

Принято от аудиторов:

1. fswatch — исправлено на --event Modified (Gemini, критично)
   Было: --event Created → срабатывал только 1 раз
   Стало: --event Modified → срабатывает при каждом обновлении questions.md

2. mypy — сделан опциональным (DeepSeek)
   ruff — всегда обязателен
   mypy — только если в AGENTS.md указано strict_type_checking: true

3. Smoke-тест добавлен в Этап 2 (DeepSeek)
   После pytest Cursor запускает smoke_test.sh
   smoke_test.sh берёт примеры вход/выход из spec.md и проверяет реальный запуск

4. Чеклист в review.md (DeepSeek)
   Claude заполняет обязательные пункты галочками:
   - [ ] Все функции из spec.md реализованы
   - [ ] Каждый edge case покрыт тестом
   - [ ] Нет заглушек (TODO, pass, NotImplementedError)
   - [ ] ruff — чисто
   - [ ] smoke_test.sh — PASS

5. Inspector дублирует fswatch (ChatGPT)
   Inspector делает poll questions.md каждые 3 минуты как резерв

6. Очистка questions.md (Gemini)
   AGENTS.md обязывает Cursor очищать questions.md после получения answers.md
   Предотвращает ложные срабатывания watcher

7. Архивация logs/ (DeepSeek)
   После завершения проекта questions.md/answers.md → logs/

---

ОТКЛОНЕНО (с обоснованием):

X. 3 итерации → считать прогресс а не количество (ChatGPT, Gemini)
   ОТКЛОНЕНО — измерить "прогресс" автоматически невозможно без дополнительного
   AI-агента который оценивает каждый ответ. Это усложнение без измеримого выигрыша.
   Оставляем счётчик 3 итерации — простой и предсказуемый лимит.
   Директор всегда может вручную дать больше итераций если видит прогресс.

---

ФИНАЛЬНОЕ РЕШЕНИЕ — ПАЙПЛАЙН 4 ЭТАПА:

ЭТАП 1: СПЕЦИФИКАЦИЯ (Claude Desktop)
  Claude пишет:
    spec.md          — требования, контракты вход/выход, edge cases, примеры I/O
    AGENTS.md        — правила Cursor: запреты, паттерны, ruff обязателен,
                       mypy: true/false, очищать questions.md после ответа
    tests_spec.py    — тесты контрактов до кода (не реализация)
  Директор утверждает → Этап 2
  Токены Claude: ~500

ЭТАП 2: РЕАЛИЗАЦИЯ (Cursor)
  Cursor читает spec.md + AGENTS.md + tests_spec.py
  Cursor выполняет в порядке:
    1. пишет код
    2. ruff (всегда) + mypy (если strict_type_checking: true)
    3. pytest → test_report.md (полный вывод)
    4. smoke_test.sh → smoke_report.md (реальный запуск с I/O из spec.md)

  При блокировке:
    Cursor пишет/обновляет questions.md (тег BLOCKED: [причина])
    fswatch --event Modified → notify_director.py → Telegram
    Inspector poll каждые 3 мин как резерв
    Claude читает questions.md → пишет answers.md
    Cursor читает answers.md → продолжает
    После 3 итераций без решения → эскалация Директору
    После ответа Cursor очищает questions.md (по AGENTS.md)

  Токены Claude: 0 (только при блокировке ~200/итерация)

ЭТАП 3: РЕВЬЮ (Claude Desktop)
  Claude читает: git diff + test_report.md + smoke_report.md
  Заполняет review.md с обязательным чеклистом:
    - [ ] Все функции из spec.md реализованы
    - [ ] Каждый edge case покрыт тестом
    - [ ] Нет заглушек (TODO, pass, NotImplementedError)
    - [ ] ruff — чисто
    - [ ] smoke_test.sh — PASS
  Оценка: ГОТОВ / НЕ ГОТОВ + список замечаний
  Если НЕ ГОТОВ → Cursor исправляет → повтор Этапа 3 (до 3 циклов)
  Токены Claude: ~400-600

ЭТАП 4: УТВЕРЖДЕНИЕ (Директор)
  Директор читает review.md + чеклист
  Утверждает → git tag release → деплой
  Токены Claude: 0

---

СТРУКТУРА ФАЙЛОВ:

~/Documents/NEVA/projects/[ИМЯ]/
  spec.md           ← Claude, Этап 1
  AGENTS.md         ← Claude, Этап 1
  tests_spec.py     ← Claude, Этап 1
  smoke_test.sh     ← Claude, Этап 1 (берёт I/O из spec.md)
  src/              ← Cursor, Этап 2
  tests/            ← Cursor, Этап 2 (дополняет tests_spec.py)
  test_report.md    ← Cursor, Этап 2
  smoke_report.md   ← Cursor, Этап 2
  questions.md      ← Cursor при блокировке (очищается после ответа)
  answers.md        ← Claude при блокировке
  review.md         ← Claude, Этап 3
  logs/             ← архив questions/answers после завершения

ТРИГГЕР:
  fswatch ~/Documents/NEVA/projects/ --event Modified \
    --exclude '\.swp$' --exclude '~$' --exclude '\.tmp$' \
    | grep questions.md \
    | xargs -I{} python3 notify_director.py "{}"
  + Inspector poll каждые 3 мин как резерв

ЭКОНОМИЯ ТОКЕНОВ:
  Было: ~5000 токенов/задача
  Стало: ~800 токенов/задача
  Экономия: 84%

---

ЗАДАНИЕ — КРУГ 4: ФИНАЛЬНАЯ ОЦЕНКА

Архитектор принял 6 из 7 замечаний Круга 3.
Отклонено 1: счётчик итераций (3) вместо оценки прогресса.

Обоснование отклонения: автоматическая оценка прогресса требует
дополнительного AI-агента — это усложнение без измеримого выигрыша.
Директор вручную может дать больше итераций если видит прогресс.

ОЦЕНИ ФИНАЛЬНОЕ РЕШЕНИЕ:

1. Исправление fswatch --event Modified — проблема решена?

2. mypy опциональный через AGENTS.md — приемлемо?

3. smoke_test.sh в Этапе 2 — правильное место и реализация?

4. Чеклист в review.md — достаточно для верификации?

5. Inspector как резерв fswatch — достаточно надёжно?

6. Отклонение счётчика итераций — согласен с обоснованием?

7. ФИНАЛЬНАЯ ОЦЕНКА:
   ГОТОВ — решение можно внедрять
   НЕ ГОТОВ — [одна блокирующая причина]
   ДОРАБОТАТЬ — [один конкретный пункт]

Отвечай на русском. Кратко.
