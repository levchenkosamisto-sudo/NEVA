# ПРОМПТ ДЛЯ ДУМЫ — ПАМЯТЬ И GIT NEVA
## Круг 1: Вопросы без оценок
**Аудит:** DUMA-MEM-GIT-2 | **Дата:** 2026-06-27

---

Ты аудитор NEVA. Тебе переданы два документа: АРХИТЕКТУРА и ТЗ. Их содержание уже включено в этот промпт.
Задача: задать вопросы по каждому блоку. Только вопросы — без оценок, без рекомендаций.

---

## СОДЕРЖАНИЕ ДОКУМЕНТОВ

### АРХИТЕКТУРА (DUMA-MEM-GIT-2-ARCH.md)

БЛОК 1 — ЕДИНАЯ ПАМЯТЬ NEVA:
Требование: одна база памяти для всех ИИ без исключений: Клод Десктоп (:9000), веб-ИИ (:9100), АПИ-вызовы.
Инструмент: Mem0 (бесплатный, локальный, SQLite).
Три слоя памяти: документы, события (ответы ДУМЫ + решения Директора), факты.
Два action: memory_search(запрос, top_k=5), memory_write(контент, source, tags).
Гибридный поиск: векторный + ключевые слова одновременно.
Контекстный пакет: перед каждым АПИ-вызовом — 5 релевантных фактов в промпт.
Автозапись: после каждой сессии Клода — сжатое резюме в память.
Рам (важно!): Mem0 ≈ 200-400 МБ, М1 8GB, не запускать с qwen (4.7 GB).

BLOK 2 — GIT КАК SSOT:
Требование: ни одно изменение не проходит мимо Гита.
Ветки: main / dev / audit/XXX / snap/XXX.
pre-commit хук: блок файлы .env, ruff check, TODO в governance/, проверка противоречий.
post-commit хук: обновить Mem0 индекс, уведомить Директора.
Проверка противоречий: скрипт neva_contradiction_check.py — сравнивает изменённый файл с Mem0.
Автокоммит: скрипт neva_autocommit.py — после завершения задачи.
Поиск по истории: git_search action — ищет через Mem0-индекс коммитов.

### ТЗ (DUMA-MEM-GIT-2-TASK.md)

Задача 1 (P0): создать neva_memory.py + action memory_search/memory_write в :9000 и :9100 + neva_indexer.py.
Задача 2 (P0): создать neva_contradiction_check.py + зарегистрировать в pre-commit hook.
Задача 3 (P1): ветки main/dev/audit/snap + neva_autocommit.py + pre-commit hook.
Задача 4 (P2): neva_git_index.py + action git_search.

---

## ЗАДАНИЕ ДЛЯ АУДИТОРА

Структура ответа:
АУДИТОР: [deepseek / gemini / chatgpt]
ДАТА: 2026-06-27

БЛОК 1 — ПАМЯТЬ:
В1.1: [вопрос]
В1.2: [вопрос]
В1.3: [вопрос]

БЛОК 2 — GIT КАК SSOT:
В2.1: [вопрос]
В2.2: [вопрос]
В2.3: [вопрос]

БЛОК 3 — ПРОТИВОРЕЧИЯ:
В3.1: [вопрос]
В3.2: [вопрос]
В3.3: [вопрос]

ОБЩИЕ ВОПРОСЫ:
ВО.1: [вопрос]
ВО.2: [вопрос]

---

## ДОПОЛНИТЕЛЬНО: КАК ПЕРЕДАТЬ ОТВЕТ

**DeepSeek (MCP SuperAssistant):**
Используй write_result с auditor=deepseek.

**Gemini (MCP SuperAssistant или локальный скрипт):**
Если MCP не срабатывает — Архитектор запускает локальный Питон-скрипт:
  python3 neva_duma_sender.py --model gemini --doc DUMA-MEM-GIT-2-ARCH.md --doc DUMA-MEM-GIT-2-TASK.md
Скрипт передаёт содержание файлов как контекст, получает ответ, сам записывает через write_result.

**ChatGPT (прямой текст в чат):**
Архитектор копирует ответ вручную и записывает через write_result.
